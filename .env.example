# Copy to .env and fill in. .env is gitignored and must never be committed.
# Only the variable NAME ever appears in the code, logs or error messages.

# Default provider (agent/config.py: PROVIDER = "gemini")
GEMINI_API_KEY=

# Only needed if you set PROVIDER = "claude"
# ANTHROPIC_API_KEY=
